FROM python:3.10-slim

WORKDIR /app

# Installiamo le librerie necessarie
RUN pip install mcp docker paho-mqtt influxdb-client

# Copiamo lo script del server MCP
COPY mcp_edge_recovery.py .

# Il comando di avvio
CMD ["python", "-u", "mcp_edge_recovery.py"]