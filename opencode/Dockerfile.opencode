FROM oven/bun:latest
RUN bun install -g opencode-ai
WORKDIR /root
RUN mkdir -p /root/.config/opencode
COPY opencode.json /root/.config/opencode/opencode.json
EXPOSE 4096
CMD ["opencode", "serve", "--port", "4096", "--hostname", "0.0.0.0", "--cors", "*"]
